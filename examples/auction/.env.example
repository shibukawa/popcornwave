# Docker Compose turns these host values into mounted secrets. Do not put a
# real token in this tracked example; prefer a shell or CI secret store.
SPLUNK_REALM=jp0
SPLUNK_ACCESS_TOKEN=
OIDC_CLIENT_SECRET=
SESSION_KEYRING_SECRET=
AUCTION_UID=501
AUCTION_GID=20

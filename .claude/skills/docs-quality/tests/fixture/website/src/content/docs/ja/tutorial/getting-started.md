---
title: 1. はじめの一歩
description: プロジェクトを作り、動かし、足場が書いたページを書き換える。
sidebar:
  order: 1
---

Tailwind は入れていない。

```html
// handlers/home.pw.html
package handlers

export component Home(name: string): html {
  <h1 class="text-3xl font-bold">Hello, {name}</h1>
}
```

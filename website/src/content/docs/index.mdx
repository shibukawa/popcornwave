---
title: Popcorn Wave
description: A small, TinyGo-oriented web application framework for Go, built directly on net/http.
template: splash
# Without this the browser tab reads "Popcorn Wave | Popcorn Wave".
head:
  - tag: title
    content: Popcorn Wave — a small web application framework for Go
hero:
  tagline: Server-rendered web applications on plain net/http, with typed templates and typed SQL generated ahead of time.
  image:
    file: ../../assets/logo.png
  actions:
    # Site-root paths, like every other internal link. src/components/Hero.astro
    # applies `base`, which the Markdown base-links plugin cannot do here
    # because hero actions come from frontmatter.
    - text: Get started
      link: /tutorial/getting-started/
      icon: right-arrow
    - text: View on GitHub
      link: https://github.com/shibukawa/popcornwave
      icon: external
      variant: minimal
---

import { Card, CardGrid } from '@astrojs/starlight/components';

Start with the unit Go already gives you: one `http.Handler`. It binds a
request, runs application logic, and writes a complete response through ordinary
navigation and form semantics. The result remains a small, practical TinyGo
target — without a component graph, patch protocol, hydration step, or browser
runtime.

<CardGrid>
  <Card title="Standard library first" icon="seti:go">
    Routing uses `net/http`'s own `ServeMux`: the same patterns, wildcards, and
    precedence. Handlers remain plain `http.HandlerFunc`.
  </Card>
  <Card title="Generated, not reflected" icon="setting">
    `pw generate` compiles templates, SQL, request binding, and OpenAPI into Go
    ahead of time. With no runtime reflection, the same code runs on
    **TinyGo**.
  </Card>
  <Card title="One command to a running app" icon="rocket">
    `pw init` scaffolds a complete project — handler, typed page, document
    shell, SQL, migration, error pages, Devbox — and `pw dev` watches it.
  </Card>
  <Card title="Operations included" icon="approve-check">
    Startup validation, `/healthz`, `/readyz`, `/openapi.json`, graceful
    shutdown, request IDs, access logs, and validated security headers.
  </Card>
</CardGrid>

Need richer interactions later? An opt-in server-driven UI layer extends the
same `net/http` foundation rather than replacing it. If an application does not
import that layer, it does not pay for it.

## The same todo app, measured twice

[`examples/todo`](https://github.com/shibukawa/popcornwave/tree/main/examples/todo)
holds one todo list written twice against one PostgreSQL table — once with
`net/http`, `html/template`, `encoding/json` and `pgx`, once with Popcorn Wave.
Same five routes, same rows, and the harness refuses to measure them if their
JSON bodies disagree.

Binary size, Go 1.26.5 and TinyGo 0.41.1 on darwin/arm64:

| Build | `net/http` + pgx | Popcorn Wave |
| --- | --- | --- |
| host `go build` | 18.98 MB | 19.66 MB |
| host `-ldflags="-s -w"` | 13.06 MB | 13.57 MB |
| TinyGo native | does not build | 6.13 MB |
| TinyGo native `-no-debug` | does not build | 6.13 MB |
| TinyGo `wasip1` | does not build | does not build |

The TinyGo rows are the point. `pgx` does not compile there at all — it reaches
for `tls.Conn`, `tls.X509KeyPair` and `net.Resolver`, none of which TinyGo has —
so the framework's rebuilt driver stack is what makes the column exist, and it
more than halves the binary against a stripped host build.

Neither service reaches `wasip1`, and for the framework that is a platform
limit rather than missing work: the PostgreSQL engine requires TinyGo's threaded
scheduler, because under the cooperative one a blocking socket call holds the
runtime and a query outlives its context deadline in silence. WASI preview 1 has
no threads, so the engine refuses to build there — by way of a deliberate
compile error whose identifier names the fix.

Throughput, k6 at 20 VUs against the same seeded 50 rows, medians of five
alternating passes. The framework result depends almost entirely on where the
session lives:

| | requests/s | HTML p95 |
| --- | --- | --- |
| `net/http` + pgx | ~14,700 | 3.15–3.39 ms |
| Popcorn Wave, cookie session | ~13,700 | 2.90–3.21 ms |
| Popcorn Wave, server-side session | ~15,800 | 2.46–2.66 ms |

Same application code in both framework rows, and the framework is quicker to
first byte in both. A cookie session puts the whole record on the wire twice per
request — 631 bytes of `Set-Cookie` against 299 — and that, not the AES-GCM
sealing it, is what costs the throughput: the crypto measures 0.5 µs.

Per request the framework spends **203 µs of CPU against the baseline's 221**,
while also running a session, a CSRF check, security headers and a request ID
the hand-written service does not have. Its whole middleware chain is **2.7 µs**,
against **37 µs** for one loopback query.
[Performance](/guides/architecture/performance/) has the full breakdown and the
four other settings worth tuning.

[Why Popcorn Wave](/start/why-popcorn-wave/) explains how that choice connects
productivity, performance, and interoperability.
